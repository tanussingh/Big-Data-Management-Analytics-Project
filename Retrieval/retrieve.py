import pymongo
from pymongo import MongoClient
import json

def main():
    # Set up connection to Mongo Server
    client = MongoClient("localhost", 27017)

    # Set connection to database
    collection_Article = client[simple]

    #

if __name__ == "__main__"
    main()
